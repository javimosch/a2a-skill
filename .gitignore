# Go build artifacts
/dist/
*.exe
*.test
*.out
.coverage

# Cached python interpreter path written by the bash wrapper
.a2a_python

# Per-project databases live under ~/.a2a, never in the repo
.a2a/
*.db
*.db-wal
*.db-shm

# Smoke-test logs
*.log
/tmp/a2a-*

# Editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp

# Rust build artifacts
/target/
Cargo.lock

# Python
__pycache__/
*.pyc
*.pyo

# Node.js (web UI / examples)
node_modules/
package-lock.json

# All .agents/skills/ subdirs except the generic a2a usage skill
# Dev-process skills live at ~/ai/a2a-dev/ — not in this repo
.agents/skills/*/
!.agents/skills/a2a/

# Claude Code session state
.claude/

# Devin / AI platform artifacts
.devin/

# Collaborative artifact build outputs (checked in: build scripts + artifacts)
palette_preview.html

# Compiled a2a test binary (generated by Nuitka build)
a2a_test